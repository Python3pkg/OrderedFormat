sudo: false

language: python

env:
  matrix:
   - TOXENV=py27
   - TOXENV=py33
   - TOXENV=py34
install:
  - pip install tox
  - pip install coveralls
script: tox
after_script:
  - if test "$TOXENV" = py34 ; then coveralls ; fi